['cron_[a-f0-9]+]\' /root/.hermes/profiles/<agent>/logs/agent.log | tail -20`\n   - Per-agent cron output: `ls -lat /root/.hermes/profiles/<agent>/cron/output/`\n   - Session records: `find /root/.hermes/profiles/<agent>/sessions -name "session_cron_*', 'check timestamps\n\nIf scripts exist but **no cron entries** in either system or internal registry **and no execution evidence**, the agent is idle due to missing cron configuration.\n\n**Recovery** (preferred):\n\n```bash\nhermes cron create --profile <agent> --name "<descriptive name>" --schedule "<crontab>" --script "/root/.hermes/profiles/<agent>/scripts/<script>.py', "Alternative** (system crontab):\n\n```\n*/10 * * * * HERMES_HOME=/root/.hermes/profiles/<agent> /usr/bin/env python3 /root/.hermes/profiles/<agent>/scripts/<script>.py >> /root/.hermes/profiles/<agent>/cron.log 2>&1\n```\n\n**Pitfall**: Do not mix both mechanisms for the same script — causes duplicate execution (see `references/2026-05-04-yoyo-duplicate-cron-detection.md`).\n\n**Gentech-specific**: The Gentech profile had an active gateway but no cron jobs registered. Recovery requires either creating an internal Hermes cron job or adding a system crontab entry for the desired automation script(s).\n\n**Related**: YoYo's simultaneous AAE config path error (`references/2026-05-04-yoyo-hermes-home-path-fragmentation.md`) illustrates that even with cron execution present, HOME path issues can disrupt job success."]