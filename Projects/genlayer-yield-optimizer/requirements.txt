# GenLayer Yield Optimizer — Dependencies
# Core (required for contract deployment)
genlayer-js>=1.0.0

# CLI & Monitoring (standalone — no GenLayer SDK needed)
# monitor.py and scan mode of cli.py work without genlayer-js

# Testing
pytest>=7.0.0
